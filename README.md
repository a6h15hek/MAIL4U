# MAIL4U A Short Message Sending Application
A Java Desktop application uses SQL Database. It have Sign In & Sign Up for user. View all send & received email. Email can be sent through unique username.

